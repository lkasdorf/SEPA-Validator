# Normalize line endings deterministically, regardless of each contributor's
# core.autocrlf setting.

# Shell scripts must use LF — CRLF breaks them under bash/WSL.
*.sh text eol=lf

# Windows-only scripts stay CRLF.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf
